export interface Theme {
    primary: string,
    secondary: string,
    background: string,
    surface: string,
    error: string,
    onPrimary: string, //font color on primary
    onSecondary: string, //font color on secondary
    onBackground: string, //font color on background
    onSurface: string, //font color on surface
    onError: string, //font color on error
}